"use client";

import { createElement, useEffect } from "react";

const INTRO_SPLINE_URL =
  "https://prod.spline.design/NTdgt7MITavRr7dR/scene.splinecode";

const CUBE_SPLINE_URL =
  "https://prod.spline.design/GkLXNL9XsUfs93IY/scene.splinecode";

const SCENE_HEIGHT = 1800;
const STORY_SECTIONS = 4;
const STORY_HEIGHT = SCENE_HEIGHT * STORY_SECTIONS;

type TextSectionProps = {
  id: string;
  eyebrow: string;
  title: string;
  text: string;
  side: "left" | "right" | "center";
};

function SplineViewerScene({ url }: { url: string }) {
  useEffect(() => {
    const scriptId = "spline-viewer-script";

    if (!document.getElementById(scriptId)) {
      const script = document.createElement("script");
      script.id = scriptId;
      script.type = "module";
      script.src =
        "https://unpkg.com/@splinetool/viewer@1.12.96/build/spline-viewer.js";

      document.body.appendChild(script);
    }
  }, []);

  return createElement("spline-viewer" as any, {
    url,
    style: {
      width: "100%",
      height: "100%",
      display: "block",
    },
  });
}

function IntroSection() {
  return (
    <section
      id="inicio"
      className="relative h-screen overflow-hidden bg-[#F7F9FC] px-6 md:px-12 lg:px-[7vw]"
    >
      <div className="absolute inset-0 z-[5]">
        <SplineViewerScene url={INTRO_SPLINE_URL} />
      </div>

      <div className="pointer-events-none absolute inset-0 z-[10] bg-[radial-gradient(circle_at_50%_45%,rgba(0,213,232,0.18),transparent_38%)]" />
      <div className="pointer-events-none absolute inset-0 z-[11] bg-[linear-gradient(90deg,rgba(247,249,252,0.92)_0%,rgba(247,249,252,0.55)_45%,rgba(247,249,252,0.86)_100%)]" />

      <div className="relative z-[20] flex h-full items-end pb-[11vh]">
        <div className="max-w-6xl">
          <h1 className="text-[52px] font-black leading-[0.9] tracking-[-0.075em] text-[#071014] md:text-[84px] lg:text-[104px]">
            Rubik Studio
            <br />
            Potenciamos tu marca.
          </h1>

          <div className="mt-8 flex flex-wrap gap-5 text-xs font-semibold uppercase tracking-[0.35em] text-[#5E6875] md:text-sm">
            <span>Branding</span>
            <span className="text-[#00D5E8]">\</span>
            <span>Gráfica</span>
            <span className="text-[#00D5E8]">\</span>
            <span>Stands</span>
            <span className="text-[#E83A8C]">\</span>
            <span>Señalética</span>
            <span className="text-[#E83A8C]">\</span>
            <span>Implementación</span>
          </div>
        </div>
      </div>

      <div className="absolute right-6 bottom-[16vh] z-[20] hidden max-w-sm lg:right-[7vw] lg:block">
        <p className="text-sm leading-7 text-[#5E6875]">
          Creamos soluciones visuales, gráficas y constructivas para marcas que
          necesitan destacar en espacios reales.
        </p>

        <div className="mt-8 flex items-center gap-3">
          <a
            href="#contacto"
            className="rounded-full border border-[#DCE4EA] bg-white px-7 py-4 text-sm font-bold text-[#071014] shadow-[0_12px_35px_rgba(15,23,42,0.08)] transition hover:border-[#00D5E8] hover:text-[#00AFC0]"
          >
            Contáctanos
          </a>

          <a
            href="#quienes-somos"
            className="rounded-full border border-[#DCE4EA] bg-white px-7 py-4 text-sm font-bold text-[#071014] shadow-[0_12px_35px_rgba(15,23,42,0.08)] transition hover:border-[#E83A8C] hover:text-[#E83A8C]"
          >
            Ver historia
          </a>

          <a
            href="#contacto"
            className="flex h-12 w-12 items-center justify-center rounded-full bg-[#00D5E8] text-2xl font-black text-white shadow-[0_14px_40px_rgba(0,213,232,0.35)] transition hover:bg-[#E83A8C]"
          >
            +
          </a>
        </div>
      </div>
    </section>
  );
}

function ContenedorDel3D() {
  return (
    <div className="sticky top-0 z-[40] h-screen w-full overflow-hidden bg-[#F7F9FC]">
      <div className="absolute inset-0 z-[5] bg-[radial-gradient(circle_at_38%_50%,rgba(0,213,232,0.18),transparent_34%)]" />
      <div className="absolute inset-0 z-[6] bg-[radial-gradient(circle_at_70%_52%,rgba(232,58,140,0.10),transparent_34%)]" />

      <div className="absolute left-1/2 top-0 z-[50] h-screen w-full -translate-x-1/2 overflow-visible">
        <div className="absolute left-[7vw] top-1/2 h-[70vh] w-[44vw] -translate-y-1/2 overflow-visible">
          <SplineViewerScene url={CUBE_SPLINE_URL} />
        </div>
      </div>

      <div className="pointer-events-none absolute inset-0 z-[60] bg-[linear-gradient(90deg,rgba(247,249,252,0.2)_0%,rgba(247,249,252,0.58)_48%,rgba(247,249,252,0.96)_100%)]" />
    </div>
  );
}

function TextSection({ id, eyebrow, title, text, side }: TextSectionProps) {
  const position =
    side === "left"
      ? "items-start text-left md:pl-[7vw]"
      : side === "right"
        ? "items-end text-left md:pr-[7vw]"
        : "items-center text-center";

  return (
    <section
      id={id}
      className={`relative z-[80] flex flex-col justify-center px-6 md:px-12 ${position}`}
      style={{ height: `${SCENE_HEIGHT}px` }}
    >
      <div className="max-w-xl rounded-[34px] border border-[#DCE4EA]/70 bg-white/72 p-8 shadow-[0_24px_80px_rgba(15,23,42,0.08)] backdrop-blur-xl md:p-10">
        <p className="mb-5 text-xs font-black uppercase tracking-[0.42em] text-[#00AFC0] md:text-sm">
          {eyebrow}
        </p>

        <h2 className="text-[38px] font-black leading-[0.94] tracking-[-0.055em] text-[#071014] md:text-6xl lg:text-[72px]">
          {title}
        </h2>

        <p className="mt-7 max-w-2xl text-base leading-8 text-[#5E6875] md:text-lg">
          {text}
        </p>
      </div>
    </section>
  );
}

function StoryScene() {
  return (
    <section
      className="relative bg-[#F7F9FC]"
      style={{ height: `${STORY_HEIGHT}px` }}
    >
      <ContenedorDel3D />

      <div className="pointer-events-none relative z-[70] -mt-[100vh]">
        <TextSection
          id="quienes-somos"
          eyebrow="Quiénes somos"
          title="Un estudio creativo con capacidad real de producción."
          text="Diseñamos, producimos e implementamos soluciones visuales para que las marcas no solo se vean bien, sino que funcionen en espacios reales."
          side="right"
        />

        <TextSection
          id="que-hacemos"
          eyebrow="Qué hacemos"
          title="Gráfica, branding, señalética, stands e implementación."
          text="Transformamos identidad visual en piezas tangibles: adhesivos, volumétricos, neón flex, pendones, vitrinas, fachadas, activaciones y espacios comerciales."
          side="left"
        />

        <TextSection
          id="como"
          eyebrow="Cómo trabajamos"
          title="Del concepto a la instalación final."
          text="Levantamos la necesidad, definimos una solución visual, producimos con criterio técnico y ejecutamos en terreno cuidando terminaciones y experiencia."
          side="right"
        />

        <TextSection
          id="cuanto"
          eyebrow="Cuánto"
          title="Cada proyecto se cotiza según alcance real."
          text="El valor depende de medidas, materialidad, terminaciones, cantidad, instalación, logística y complejidad del montaje."
          side="center"
        />
      </div>
    </section>
  );
}

export default function ScrollExperience() {
  return (
    <>
      <IntroSection />
      <StoryScene />
    </>
  );
}